{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'echo \"[Deploy] Starting TradeUp v2.0.2...\" && echo \"[Deploy] Skipping migrations (use /api/admin/fix-schema to fix DB)...\" && echo \"[Deploy] Starting gunicorn...\" && gunicorn -c gunicorn.conf.py run:app'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
